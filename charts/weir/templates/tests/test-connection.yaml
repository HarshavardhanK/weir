apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "weir.fullname" . }}-test-connection"
  labels:
    {{- include "weir.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "weir.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
